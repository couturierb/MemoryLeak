{
  "name": "memoryleak",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "emulator": "firebase emulators:start --only firestore --import=dev-data --export-on-exit=dev-data",
    "proxy": "node proxy/proxy.js",
    "build": "vite build"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "firebase": "^9.6.6",
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.0.0",
    "cors-anywhere": "^0.4.4",
    "firebase-tools": "^10.6.0",
    "vite": "^2.7.2"
  }
}
